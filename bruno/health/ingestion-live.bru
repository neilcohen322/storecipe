meta {
  name: Ingestion liveness
  type: http
  seq: 3
}

get {
  url: {{ingestion_url}}/health/live
  body: none
  auth: none
}

tests {
  test("ingestion is alive", function () {
    expect(res.status).to.equal(200);
  });
}

